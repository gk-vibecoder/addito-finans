---
import BaseLayout from '@/layouts/BaseLayout.astro';
import CTA from '@/components/sections/CTA.astro';
import { seoEn } from '@/config/seo.en';
import { AnimatedSection } from '@/components/ui/AnimatedSection';
---

<BaseLayout
  title={seoEn.financialServices.title}
  description={seoEn.financialServices.description}
  alternateHref="/financial-services"
>

  <!-- ─── Custom hero: split layout ──────────────────────────────────────────── -->
  <section class="mx-auto max-w-6xl px-6 pt-16 pb-12 md:pt-24">
    <AnimatedSection client:visible slow className="grid md:grid-cols-5 gap-12 items-start">

      <!-- Left 3 cols: headline + intro -->
      <div class="md:col-span-3">
        <p class="text-xs font-semibold tracking-widest uppercase text-muted-foreground mb-6">Financial Services</p>
        <h1 class="text-4xl md:text-5xl font-bold tracking-tight text-foreground leading-tight max-w-lg">
          Financial solutions for every stage of your business.
        </h1>
        <p class="mt-6 text-lg text-muted-foreground leading-relaxed max-w-md">
          Strengthen your cash flow with invoice factoring or secure the capital you need to grow. We are your independent advisor — we work for you.
        </p>
        <div class="mt-8 flex flex-wrap gap-3">
          <a href="#factoring" class="inline-flex h-9 items-center justify-center rounded-md bg-primary px-4 text-sm font-medium text-primary-foreground hover:bg-primary/90 transition-colors">
            Invoice Factoring
          </a>
          <a href="#financing" class="inline-flex h-9 items-center justify-center rounded-md border border-border px-4 text-sm font-medium text-foreground hover:bg-accent transition-colors">
            Business Financing
          </a>
        </div>
      </div>

      <!-- Right 2 cols: stat cards -->
      <div class="md:col-span-2 flex flex-col gap-4 md:pt-8">
        <div class="rounded-xl border border-border bg-card p-6">
          <p class="font-mono text-4xl font-bold text-foreground">90%</p>
          <p class="text-xs text-muted-foreground mt-1">of invoice value paid out immediately</p>
        </div>
        <div class="rounded-xl border border-border bg-card p-6">
          <p class="font-mono text-4xl font-bold text-foreground">24h</p>
          <p class="text-xs text-muted-foreground mt-1">average payout time</p>
        </div>
        <div class="rounded-xl border border-primary/20 bg-primary/5 p-6">
          <p class="font-mono text-4xl font-bold text-primary">0 kr</p>
          <p class="text-xs text-muted-foreground mt-1">no collateral required for invoice factoring</p>
        </div>
      </div>

    </AnimatedSection>
  </section>

  <!-- ─── Factoring: split section ───────────────────────────────────────────── -->
  <section id="factoring" class="border-t border-border">
    <div class="mx-auto max-w-6xl px-6 py-16 md:py-24">
      <AnimatedSection client:visible className="grid md:grid-cols-2 gap-12 md:gap-16 items-start">

        <!-- Left: content -->
        <div>
          <p class="font-mono text-xs text-muted-foreground mb-6 uppercase tracking-widest">01 — Invoice Factoring</p>
          <h2 class="text-3xl font-bold tracking-tight text-foreground mb-6">
            Stop financing your customers' payment terms.
          </h2>
          <p class="text-muted-foreground leading-relaxed mb-8">
            You invoice your customer as normal. We pay out up to 90% of the invoice value immediately — no waiting 30, 60, or 90 days. We handle the collection.
          </p>
          <ul class="space-y-3">
            {[
              'Payout within 24 hours',
              'Up to 90% of invoice value',
              'No collateral or guarantees required',
              'We handle collection',
              'Transparent flat fee model',
            ].map((item) => (
              <li class="flex items-center gap-3 text-sm text-muted-foreground">
                <span class="size-1.5 rounded-full bg-primary shrink-0" />
                {item}
              </li>
            ))}
          </ul>
        </div>

        <!-- Right: how it works steps -->
        <div class="flex flex-col gap-0">
          <p class="text-xs font-semibold tracking-widest uppercase text-muted-foreground mb-8">How it works</p>
          {[
            { step: '01', title: 'You invoice the customer', body: 'Send the invoice as normal and upload a copy to us. The process takes under five minutes.' },
            { step: '02', title: 'We pay out immediately', body: 'Within 24 hours, up to 90% of the invoice value is in your account. No questions, no waiting.' },
            { step: '03', title: 'We handle the payment', body: 'When the customer pays us, we settle the remaining amount minus our fee. Simple and transparent.' },
          ].map(({ step, title, body }, i) => (
            <div class:list={['flex gap-5 pb-8 ml-4', i < 2 && 'border-l border-border']}>
              <div class="flex flex-col items-center shrink-0 -ml-4">
                <div class="size-8 rounded-full border border-border bg-background flex items-center justify-center shrink-0">
                  <span class="font-mono text-xs text-muted-foreground">{step}</span>
                </div>
              </div>
              <div class="pb-2">
                <p class="font-semibold text-foreground text-sm mb-1">{title}</p>
                <p class="text-sm text-muted-foreground leading-relaxed">{body}</p>
              </div>
            </div>
          ))}
        </div>

      </AnimatedSection>
    </div>
  </section>

  <!-- ─── Financing: flipped split section ───────────────────────────────────── -->
  <section id="financing" class="border-t border-border bg-muted/20">
    <div class="mx-auto max-w-6xl px-6 py-16 md:py-24">
      <AnimatedSection client:visible className="grid md:grid-cols-2 gap-12 md:gap-16 items-start">

        <!-- Left: financing type cards -->
        <div class="flex flex-col gap-4 md:order-1">
          {[
            {
              icon: '◈',
              title: 'Business loans',
              body: 'Capital for investments, expansion, or working capital. We compare terms from multiple lenders and help you negotiate.',
            },
            {
              icon: '⬡',
              title: 'Credit lines',
              body: 'Flexible credit for temporary liquidity needs. You only pay interest on what you actually use.',
            },
            {
              icon: '◆',
              title: 'Debt advisory',
              body: 'Do you have existing loans with unfavourable terms? We analyse your debt situation and identify restructuring opportunities.',
            },
          ].map(({ icon, title, body }) => (
            <div class="rounded-xl border border-border bg-card p-5 flex gap-4 items-start">
              <div class="size-9 rounded-lg bg-muted flex items-center justify-center shrink-0 mt-0.5">
                <span class="text-foreground text-sm">{icon}</span>
              </div>
              <div>
                <p class="font-semibold text-foreground text-sm">{title}</p>
                <p class="text-xs text-muted-foreground mt-1 leading-relaxed">{body}</p>
              </div>
            </div>
          ))}
        </div>

        <!-- Right: content -->
        <div class="md:order-2">
          <p class="font-mono text-xs text-muted-foreground mb-6 uppercase tracking-widest">02 — Business Financing</p>
          <h2 class="text-3xl font-bold tracking-tight text-foreground mb-6">
            The right capital. On your terms.
          </h2>
          <p class="text-muted-foreground leading-relaxed mb-6">
            We are independent advisors — we work for you, not the banks. That means you always get honest advice about which financing option actually suits your business.
          </p>
          <p class="text-muted-foreground leading-relaxed mb-8">
            From analysing your capital needs to negotiating terms with lenders — we are with you every step of the way.
          </p>
          <a
            href="/en/contact"
            class="inline-flex h-9 items-center justify-center rounded-md bg-primary px-5 text-sm font-medium text-primary-foreground hover:bg-primary/90 transition-colors"
          >
            Book a meeting
          </a>
        </div>

      </AnimatedSection>
    </div>
  </section>

  <!-- ─── Stats strip ──────────────────────────────────────────────────────────── -->
  <section class="border-t border-border">
    <AnimatedSection client:visible className="mx-auto max-w-6xl px-6 py-12">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-px bg-border rounded-xl overflow-hidden">
        {[
          { value: '90%', label: 'Invoice value paid out immediately' },
          { value: '24h', label: 'Average payout time' },
          { value: '0 kr', label: 'No collateral required' },
          { value: '100%', label: 'Independent advisory' },
        ].map(({ value, label }) => (
          <div class="bg-card px-6 py-8 flex flex-col gap-1">
            <p class="font-mono text-3xl font-bold text-foreground">{value}</p>
            <p class="text-xs text-muted-foreground leading-snug">{label}</p>
          </div>
        ))}
      </div>
    </AnimatedSection>
  </section>

  <CTA ctaHref="/en/contact" showForm={false} />

</BaseLayout>
